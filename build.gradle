plugins {
  id "com.hkmc.cloudnative" version "1.2.0"
}

group = "com.hkmc.ccs"
version = "1.0.4"

cloudnative {

  core {
    processcore = true
  }

  microservice {
    swagger = true
    undertow = true
    security = true
    metric = true
    trace = true
    config = true
    openfeign = true
    kafka = true
    cloudbus = true
  }

  persistent {
    jpa = true
    postgresql = true
  }

  quality {
    checkerframework = true
    checkstyleIgnoreFailure = true
    pmdIgnoreFailures = true
    sonarqubeIgnoreFailure = true
    skipCheckerFramework = true
  }

  thirdparty {
    commonsLang = true
    commonsCollections = true
    jasypt = true
  }

  cicd {
    helm = true
    gitlabPath = "ccs/domain/ccsp20-metering"
    chartName = "metering"

    jib = true
    to = "registry.hcloud.hmc.co.kr/ccs/${project.name}"
    tag = ["${project.version}", 'latest']
  }
}

dependencies {
  implementation 'io.micrometer.prometheus:prometheus-rsocket-spring:1.3.0'
}

